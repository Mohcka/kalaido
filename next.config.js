/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "tailwindui.com",
      "oaidalleapiprodscus.blob.core.windows.net",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig
